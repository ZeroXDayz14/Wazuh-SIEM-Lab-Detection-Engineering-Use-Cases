# Wazuh Configuration Notes

This folder contains sanitized configuration excerpts used in the Ubuntu detect-and-respond lab.

Included:
- SSH log monitoring via `/var/log/auth.log`
- `firewall-drop` active response command
- automatic IP blocking for brute-force detection (rule 5551)

Sensitive values were removed before publication.
